## Pipeline diagrams

Uses graphviz. Run:

```bash
bash process.sh
```

## Architecture diagram

Uses the Python [diagrams](https://diagrams.mingrammer.com/) package. Run:

```bash
uv run main.py
```
